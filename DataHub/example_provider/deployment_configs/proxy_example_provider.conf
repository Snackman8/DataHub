# --- copy this file to /etc/apache2/proxy-configs

# Proxy settings for /example_provider
ProxyPreserveHost On
ProxyPass /example_provider http://localhost:8000/
ProxyPassReverse /example_provider http://localhost:8000/
